name="garvit"
# String are immutable we can not add another character in string we changes the string they are immutable in pyhon 
# name[0] = "R"  qw can not do that in this it will give us error because string are immutable 

# len(string name) this function or method gives us the length of the string 
# stringName.upper function returns the string in uppercase
# stringName.lower() function returns the string in lowercase
# stringName.capitalize() function returns the string with first character in uppercase and the rest in lowercase
# stringName.title() function returns the string with first character of every word in string with uppercase 
# stringName.strip() function is same as trim function in javascript 
# stringName.lstirp() function removes the whitespaces from left and returns the new string 
# stringName.rstrip() function removes the whitespaces from the right and returns the new string 
# stringName.find("char") function returns the index of character in the given string index of first character
# stringName.replace("char" , "newChar") function replace all the given substring with the new given substring in the string and returns the new string 
# stingName.split(",") it splits the string based on the given character whenever that character appear it makes a new string from there it creates multiple string if the given character appears multiple times it makes the array of string 
# ",".join([array of string]) it mix the array of string into a single string and add the given character or string between them
# we call array as list in python 
# StingName.isalpha()  returns a boolean data it checks that all the character in the given string is alphabet or not 
# StringName.isdigit() returns a boolean data it checks that all the character in the given string is numbers or not
# stringName.isalnum() returns a boolean data it checks that the string is alphaNumeric or not
# stingName.isSpace()  returns a boolean data it checks that the string contain space or not
