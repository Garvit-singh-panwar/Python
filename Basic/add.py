print("program which takes sum of 2 number")
a = int(input("Enter 1st number "))
b = int( input("Enter 2nd number "))
print(a+b)
# by default the data we get from the input is in string so we have to convert it into integer 
# thats why we wrap the input inside int function so that the data we get is in the integer we call it typecasting means converting one datatype into another